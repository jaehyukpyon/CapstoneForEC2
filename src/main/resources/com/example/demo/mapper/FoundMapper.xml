<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.FoundMapper">

    <insert id="saveFound" parameterType="com.example.demo.domain.Found">
        insert
        into found(foundId, userId, title, content, image, time, registerAt, updatedAt)
        values(#{foundId}, #{userId}, #{title}, #{content}, #{image}, #{time}, #{registerAt}, #{updatedAt})
    </insert>

    <update id="updateFound">
        update found
        set
        title=#{foundDto.title},
        content=#{foundDto.content},
        image=#{foundDto.image},
        updatedAt=#{foundDto.updatedAt},
        userId=#{foundDto.userId},
        time=#{foundDto.time}
        where foundId=#{foundId}
    </update>

    <delete id="deleteFound">
        delete from found where foundId=#{foundId}
    </delete>

</mapper>